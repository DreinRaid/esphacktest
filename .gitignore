# Локальный «второй замок» для админки — не коммить, не попадёт на Pages
data/admin-gate.json
admin.key

# Не деплоим зависимости и локальные секреты админ-прокси
server/admin-api/node_modules/
server/admin-api/package-lock.json
server/admin-api/.env

# Собранные прошивки для веб-прошивальщика (подставляются в CI, локально можно положить вручную)
firmware/*.bin

server/
admin/
data/admin-gate.json
admin.key